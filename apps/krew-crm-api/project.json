{
  "root": "apps/krew-crm-api",
  "sourceRoot": "apps/krew-crm-api/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/node:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/krew-crm-api",
        "main": "apps/krew-crm-api/src/main.ts",
        "tsConfig": "apps/krew-crm-api/tsconfig.app.json"
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/krew-crm-api/src/environments/environment.ts",
              "with": "apps/krew-crm-api/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:execute",
      "options": {
        "buildTarget": "krew-crm-api:build"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "options": {
        "lintFilePatterns": ["apps/krew-crm-api/**/*.ts"]
      },
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/krew-crm-api"],
      "options": {
        "jestConfig": "apps/krew-crm-api/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
